# dependencies
node_modules/
.pnp
.pnp.*

# turborepo
.turbo

# next.js / build output
.next/
out/
build/
dist/

# testing
coverage/

# misc
.DS_Store
*.pem

# local env files — ignore all .env variants except committed examples
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# playwright / validation artifacts
.playwright-mcp/

# Claude Design handoffs — NEVER committed. Live locally per-app, linked from the
# spec (design_link). See root CLAUDE.md "Claude Design handoffs".
**/.design/
**/.design_system/

# legacy crema-arena design-system bundle — commit design-source only (skip binaries)
**/.design-system/**/fonts/
**/.design-system/**/*.bak-*/
.claude/settings.local.json

# tsc incremental cache from type-check runs
.claude/tsc-cache/

# graphify knowledge-graph output (derived, machine-specific)
graphify-out/
