{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../tsconfig/base.json",
  "compilerOptions": {
    "lib": ["WebWorker", "ESNext"],
    "types": [],
    "noEmit": true
  },
  "comment": "Service-worker entry only. WebWorker lib + empty types proves sw.ts is worker-safe: it must type-check with NO DOM / Node / React / Next ambient types.",
  "include": ["sw.ts"]
}
